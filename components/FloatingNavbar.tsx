/*
import Link from "next/link";
import { ChevronDown } from "lucide-react";
import { Button } from "@/components/ui/button";

export default function FloatingNavbar() {
  return (
    <header className="w-full px-6 lg:px-8 pt-4 absolute top-0 z-50">
      <nav className="max-w-7xl mx-auto bg-nav rounded-xl shadow-sm px-8 py-5 flex items-center justify-between">
        <Link href="/" className="flex items-center gap-2">
          <div className="w-7 h-7 bg-foreground rounded-full flex items-center justify-center">
            <div className="w-3 h-3 bg-white rounded-sm" />
          </div>
          <span className="text-2xl font-bold tracking-tight text-foreground">nickel</span>
        </Link>

        <div className="hidden md:flex items-center gap-6">
          <button className="flex items-center gap-1 text-base font-medium text-foreground/80 hover:text-foreground transition-colors">
            Products <ChevronDown className="w-3.5 h-3.5" />
          </button>
          <button className="flex items-center gap-1 text-base font-medium text-foreground/80 hover:text-foreground transition-colors">
            Company <ChevronDown className="w-3.5 h-3.5" />
          </button>
          <Link href="/pricing" className="text-base font-medium text-foreground/80 hover:text-foreground transition-colors">
            Pricing
          </Link>
          <Link href="/accountants" className="text-base font-medium text-foreground/80 hover:text-foreground transition-colors">
            For Accountants
          </Link>
        </div>

        <div className="flex items-center gap-4">
          <Link href="/login" className="hidden sm:block text-base font-medium text-foreground/80 hover:text-foreground transition-colors">
            Log in
          </Link>
          <Button variant="hero">Get started</Button>
        </div>
      </nav>
    </header>
  );
}
*/

export default function FloatingNavbar() { return null; }

